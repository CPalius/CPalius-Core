{#
    Liste özeti — blog ayarlarındaki uzunluk/birim ile kısaltılır.
    Beklenen değişken: post
#}
{% set listExcerpt = blog_list_excerpt(post) %}
{% if listExcerpt %}
    <p class="slw-feed-card__excerpt">{{ listExcerpt }}</p>
{% endif %}

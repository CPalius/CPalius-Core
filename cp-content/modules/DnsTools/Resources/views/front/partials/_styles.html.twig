<style>
    .dnstools-page {
        --dt-accent: var(--cp-primary, #2563eb);
        --dt-border: var(--cp-border, #e5e7eb);
        --dt-muted: var(--cp-muted, #6b7280);
        --dt-surface: var(--cp-surface, #ffffff);
        --dt-radius: 14px;
        max-width: 1120px;
        margin: 0 auto;
        padding: calc(var(--navbar-h, 0px) + 2rem) 1rem 4rem;
    }
    .dnstools-hero { margin-bottom: 2rem; }
    .dnstools-hero h1 { margin: 0 0 .5rem; font-size: clamp(1.7rem, 3vw, 2.5rem); line-height: 1.15; }
    .dnstools-hero p { margin: 0; color: var(--dt-muted); max-width: 68ch; }
    .dnstools-crumb { display: flex; flex-wrap: wrap; gap: .4rem; margin: 0 0 1rem; padding: 0; list-style: none; font-size: .85rem; color: var(--dt-muted); }
    .dnstools-crumb a { color: inherit; }
    .dnstools-search {
        display: grid; grid-template-columns: 1fr auto; gap: .75rem;
        margin-top: 1.25rem; padding: 1rem; border: 1px solid var(--dt-border);
        border-radius: var(--dt-radius); background: var(--dt-surface);
    }
    .dnstools-search input, .dnstools-search textarea, .dnstools-search select {
        width: 100%; padding: .7rem .85rem; border: 1px solid var(--dt-border);
        border-radius: 10px; font: inherit; background: #fff;
    }
    .dnstools-search textarea { min-height: 8rem; }
    .dnstools-btn {
        display: inline-flex; align-items: center; justify-content: center; gap: .4rem;
        padding: .7rem 1.1rem; border: 0; border-radius: 10px; background: var(--dt-accent);
        color: #fff; font: inherit; font-weight: 600; text-decoration: none; cursor: pointer;
    }
    .dnstools-btn--ghost { background: transparent; color: inherit; border: 1px solid var(--dt-border); }
    .dnstools-grid { display: grid; gap: 1rem; grid-template-columns: repeat(auto-fill, minmax(230px, 1fr)); }
    .dnstools-card {
        display: flex; flex-direction: column; gap: .45rem; padding: 1.1rem 1.15rem;
        border: 1px solid var(--dt-border); border-radius: var(--dt-radius);
        background: var(--dt-surface); text-decoration: none; color: inherit;
        min-height: 100%; position: relative;
    }
    .dnstools-card:hover { box-shadow: 0 8px 24px rgba(15, 23, 42, .08); transform: translateY(-2px); }
    .dnstools-card i { font-size: 1.35rem; color: var(--dt-accent); }
    .dnstools-card h3, .dnstools-card h2 { margin: 0; font-size: 1.05rem; }
    .dnstools-card p { margin: 0; color: var(--dt-muted); font-size: .9rem; }
    .dnstools-badge {
        position: absolute; top: .7rem; right: .7rem;
        font-size: .68rem; font-weight: 700; letter-spacing: .03em;
        padding: .15rem .45rem; border-radius: 999px; background: var(--dt-accent); color: #fff;
    }
    .dnstools-page--home {
        max-width: none; margin: 0; padding: var(--navbar-h, 64px) 0 4rem;
        background: var(--cp-lighter, #F7F8FA);
    }
    .dns-home-wrap { max-width: 1120px; margin: 0 auto; padding: 0 1.25rem; }
    .dns-home-hero__grid {
        display: grid; grid-template-columns: minmax(0, 1fr) 220px;
        gap: 2.5rem; align-items: center;
    }
    .dns-home-logo {
        width: 100%; max-width: 220px; height: auto; justify-self: end;
        padding: 1.1rem; border-radius: 28px;
        background: rgba(255, 255, 255, .92);
        box-shadow: 0 18px 40px rgba(0, 0, 0, .28);
    }
    .dns-home-hero {
        background:
            radial-gradient(900px 420px at 85% -10%, rgba(74, 173, 228, .35), transparent 60%),
            linear-gradient(165deg, #081420 0%, #1A2A3A 58%, #24384a 100%);
        color: #fff; padding: 4.5rem 0 3.2rem;
        border-bottom: 3px solid var(--cp-gold, #C8A86E);
    }
    .dns-home-kicker {
        margin: 0 0 .8rem; color: var(--cp-gold, #C8A86E);
        font-size: .78rem; font-weight: 700; letter-spacing: .14em; text-transform: uppercase;
    }
    .dns-home-hero h1 {
        margin: 0; max-width: 16ch; color: #fff;
        font-size: clamp(2.1rem, 5vw, 3.6rem); line-height: 1.05; letter-spacing: -.03em;
    }
    .dns-home-lead { margin: 1rem 0 0; max-width: 38rem; color: #C5D0DC; font-size: 1.08rem; line-height: 1.5; }
    .dns-home-search {
        display: flex; align-items: center; gap: .6rem;
        margin-top: 1.8rem; max-width: 720px; padding: .45rem .45rem .45rem 1rem;
        background: #fff; border-radius: 999px;
        box-shadow: 0 18px 50px rgba(0, 0, 0, .28);
    }
    .dns-home-search i { color: #8B9DAF; font-size: 1.1rem; }
    .dns-home-search input {
        flex: 1; min-width: 0; border: 0; outline: none; background: transparent;
        color: #1A2A3A; font: inherit; font-size: 1.05rem; padding: .7rem 0;
    }
    .dns-home-search button {
        border: 0; border-radius: 999px; padding: .85rem 1.35rem;
        background: #1A2A3A; color: #fff; font: inherit; font-weight: 700; cursor: pointer;
    }
    .dns-home-search button:hover { background: #0D1B2A; }
    .dns-home-picks { display: grid; grid-template-columns: repeat(4, 1fr); gap: .75rem; margin-top: 1.6rem; max-width: 720px; }
    .dns-home-picks a {
        display: flex; align-items: center; gap: .55rem;
        padding: .75rem .85rem; border-radius: 14px;
        background: rgba(255, 255, 255, .08); color: #fff; text-decoration: none;
        border: 1px solid rgba(255, 255, 255, .12);
    }
    .dns-home-picks a:hover { background: rgba(255, 255, 255, .16); }
    .dns-home-picks i { color: var(--cp-gold, #C8A86E); }
    .dns-home-results { margin-top: 1.5rem; }
    .dns-home-block { margin-top: 2.6rem; }
    .dns-home-head { display: flex; align-items: baseline; justify-content: space-between; gap: 1rem; margin-bottom: 1rem; }
    .dns-home-head h2 { margin: 0; font-size: 1.35rem; color: var(--cp-dark, #1A2A3A); }
    .dns-home-head a { color: var(--cp-primary, #4A7C9B); text-decoration: none; font-weight: 600; }
    .dns-home-grid { display: grid; grid-template-columns: repeat(4, 1fr); gap: .85rem; }
    .dns-home-card {
        display: flex; flex-direction: column; gap: .35rem;
        min-height: 148px; padding: 1.05rem 1.1rem;
        background: #fff; border: 1px solid var(--cp-border-light, #E4E7EC); border-radius: 16px;
        text-decoration: none; color: inherit;
        box-shadow: 0 1px 2px rgba(16, 24, 40, .04);
    }
    .dns-home-card:hover { border-color: #B7C6D4; transform: translateY(-2px); box-shadow: 0 12px 28px rgba(26, 42, 58, .08); }
    .dns-home-card i {
        width: 2.1rem; height: 2.1rem; display: grid; place-items: center;
        border-radius: 10px; background: #EAF3F8; color: #3A6A87; font-size: 1.05rem;
    }
    .dns-home-card strong { margin-top: .35rem; font-size: .98rem; color: #1A2A3A; }
    .dns-home-card span { color: #5A6B7D; font-size: .86rem; line-height: 1.35; }
    @media (max-width: 900px) {
        .dns-home-grid { grid-template-columns: repeat(2, 1fr); }
        .dns-home-picks { grid-template-columns: 1fr 1fr; }
    }
    @media (max-width: 640px) {
        .dns-home-hero { padding: 2.4rem 0 2rem; }
        .dns-home-hero__grid { grid-template-columns: 1fr; }
        .dns-home-logo { justify-self: start; max-width: 140px; order: -1; }
        .dns-home-search { border-radius: 18px; flex-wrap: wrap; padding: .7rem; }
        .dns-home-search button { width: 100%; }
        .dns-home-grid, .dns-home-picks { grid-template-columns: 1fr; }
    }
    .dnstools-section { margin: 2.4rem 0 0; }
    .dnstools-section h2 { margin: 0 0 1rem; font-size: 1.2rem; }
    .dnstools-stats { display: grid; grid-template-columns: repeat(auto-fit, minmax(140px, 1fr)); gap: 1rem; margin-top: 2rem; }
    .dnstools-stat { padding: 1rem; border: 1px solid var(--dt-border); border-radius: var(--dt-radius); text-align: center; }
    .dnstools-stat strong { display: block; font-size: 1.6rem; }
    .dnstools-stat span { color: var(--dt-muted); font-size: .85rem; }
    .dnstools-table { width: 100%; border-collapse: collapse; }
    .dnstools-table th, .dnstools-table td { text-align: left; padding: .7rem .55rem; border-bottom: 1px solid var(--dt-border); vertical-align: top; }
    .dnstools-results, .dnstools-status {
        margin-top: 1.5rem; padding: 1.1rem; border: 1px solid var(--dt-border);
        border-radius: var(--dt-radius); background: var(--dt-surface);
    }
    .dnstools-results[hidden], .dnstools-status[hidden] { display: none; }
    .dnstools-status.is-error { border-color: #ef4444; color: #991b1b; }
    .dnstools-cache { margin: 0 0 .75rem; font-size: .85rem; color: var(--dt-muted); }
    .dnstools-pre {
        overflow: auto; padding: .85rem; border-radius: 10px; background: #0f172a; color: #e2e8f0;
        font-size: .82rem; line-height: 1.45;
    }
    .dnstools-inbox {
        display: grid; gap: .7rem; padding: 1rem 1.1rem;
        border: 1px solid #dbeafe; border-radius: 12px; background: #eff6ff; color: #111827;
    }
    .dnstools-inbox p { margin: 0; color: #1e3a8a; }
    .dnstools-inbox__row { display: flex; flex-wrap: wrap; gap: .55rem; align-items: center; }
    .dnstools-inbox__row code {
        flex: 1 1 16rem; padding: .65rem .8rem; border-radius: 10px;
        background: #ffffff; color: #111827; border: 1px solid #bfdbfe;
        font-size: .95rem; word-break: break-all;
    }
    .dnstools-inbox__wait { font-size: .9rem; color: #1d4ed8 !important; }
    .dnstools-fields { display: grid; gap: .85rem; }
    .dnstools-fields label { display: grid; gap: .35rem; font-size: .85rem; color: var(--dt-muted); }
    .dnstools-community {
        display: grid; gap: .65rem; margin-top: 1rem; padding: 1rem 1.1rem;
        border: 1px dashed var(--dt-border); border-radius: var(--dt-radius);
        background: var(--dt-surface);
    }
    .dnstools-community[hidden] { display: none; }
    .dnstools-community p { margin: 0; color: var(--dt-muted); font-size: .9rem; }
    .dnstools-community-actions { display: flex; flex-wrap: wrap; gap: .5rem; }
    .dnstools-ask {
        display: inline; margin-left: .4rem; padding: 0; border: 0; background: none;
        font: inherit; font-size: .78rem; color: var(--dt-accent); cursor: pointer; text-decoration: underline;
    }
    .dnstools-wizard[hidden] { display: none !important; }
    .dnstools-wizard {
        position: fixed; inset: 0; z-index: 10050;
        display: flex; align-items: flex-start; justify-content: center;
        padding: 10vh 1rem 2rem;
    }
    .dnstools-wizard__backdrop {
        position: absolute; inset: 0;
        background: rgba(15, 23, 42, .62);
    }
    .dnstools-wizard__panel {
        position: relative; z-index: 1; width: min(32rem, 100%);
        padding: 1.35rem 1.4rem 1.2rem;
        border-radius: 16px;
        background: #ffffff;
        color: #111827;
        border: 1px solid #e5e7eb;
        box-shadow: 0 24px 60px rgba(15, 23, 42, .28);
    }
    .dnstools-wizard__panel h2 {
        margin: 0 0 .45rem; font-size: 1.25rem; line-height: 1.3; color: #111827;
    }
    .dnstools-wizard__panel p {
        margin: 0 0 .75rem; color: #4b5563; font-size: .95rem; line-height: 1.45;
    }
    .dnstools-wizard__topic { color: #111827 !important; }
    .dnstools-wizard__topic strong { color: #111827; }
    .dnstools-wizard__boards { display: grid; gap: .55rem; margin: 0 0 1rem; }
    .dnstools-wizard__board {
        display: block; width: 100%; text-align: left;
        padding: .8rem 1rem; border-radius: 12px;
        border: 1px solid #d1d5db; background: #f8fafc; color: #111827;
        font: inherit; font-weight: 600; cursor: pointer;
    }
    .dnstools-wizard__board:hover,
    .dnstools-wizard__board:focus-visible {
        background: #eff6ff; border-color: #2563eb; color: #1e3a8a; outline: none;
    }
    .dnstools-wizard__cancel {
        display: inline-flex; align-items: center; justify-content: center;
        padding: .55rem .9rem; border-radius: 10px;
        border: 1px solid #d1d5db; background: #ffffff; color: #374151;
        font: inherit; font-weight: 600; cursor: pointer;
    }
    .dnstools-wizard__cancel:hover { background: #f3f4f6; }
    .dnstools-related { display: flex; flex-wrap: wrap; gap: .5rem; margin-top: 2rem; }
    .dnstools-related a {
        padding: .35rem .7rem; border: 1px solid var(--dt-border); border-radius: 999px;
        text-decoration: none; color: inherit; font-size: .8rem;
    }
    .dnstools-score {
        display: flex; gap: 1.25rem; align-items: center;
        padding: 1.25rem; border-radius: var(--dt-radius); color: #fff;
        background: #0f766e;
    }
    .dnstools-page .dnstools-score h2,
    .dnstools-page .dnstools-score p,
    .dnstools-page .dnstools-score .dnstools-score__kicker { color: #fff; }
    .dnstools-score--excellent { background: #047857; }
    .dnstools-score--good { background: #0f766e; }
    .dnstools-score--warning { background: #b45309; }
    .dnstools-score--poor { background: #b91c1c; }
    .dnstools-score__dial {
        width: 7.5rem; height: 7.5rem; border-radius: 50%;
        display: grid; place-items: center; align-content: center;
        background: #fff; flex: none;
    }
    .dnstools-score__dial strong { font-size: 2.4rem; line-height: 1; color: #0f766e; }
    .dnstools-score__dial span { color: #0f766e; opacity: .85; font-size: .85rem; }
    .dnstools-page .dnstools-score--excellent .dnstools-score__dial strong,
    .dnstools-page .dnstools-score--excellent .dnstools-score__dial span { color: #047857; }
    .dnstools-page .dnstools-score--good .dnstools-score__dial strong,
    .dnstools-page .dnstools-score--good .dnstools-score__dial span { color: #0f766e; }
    .dnstools-page .dnstools-score--warning .dnstools-score__dial strong,
    .dnstools-page .dnstools-score--warning .dnstools-score__dial span { color: #b45309; }
    .dnstools-page .dnstools-score--poor .dnstools-score__dial strong,
    .dnstools-page .dnstools-score--poor .dnstools-score__dial span { color: #b91c1c; }
    .dnstools-score__kicker { opacity: .9; font-size: .85rem; }
    .dnstools-score h2 { margin: .15rem 0; font-size: 1.8rem; }
    .dnstools-score p { margin: 0; overflow-wrap: anywhere; }
    .dnstools-score__facts {
        display: grid; grid-template-columns: repeat(auto-fit, minmax(180px, 1fr));
        gap: .75rem; margin: 1rem 0;
    }
    .dnstools-score__facts div {
        padding: .75rem .9rem; border: 1px solid var(--dt-border); border-radius: 12px; background: var(--dt-surface);
    }
    .dnstools-score__facts span { display: block; color: var(--dt-muted); font-size: .75rem; }
    .dnstools-score__heading { margin: 1.25rem 0 .6rem; font-size: 1.05rem; }
    .dnstools-tips { display: grid; gap: .7rem; }
    .dnstools-tip { padding: .9rem 1rem; border-radius: 12px; border: 1px solid var(--dt-border); background: var(--dt-surface); }
    .dnstools-tip h3 { margin: 0 0 .3rem; font-size: 1rem; }
    .dnstools-tip p { margin: 0; color: var(--dt-muted); }
    .dnstools-tip--bad { border-color: #fecaca; background: #fef2f2; }
    .dnstools-tip--warn { border-color: #fde68a; background: #fffbeb; }
    .dnstools-tip--ok { border-color: #bbf7d0; background: #f0fdf4; }
    .dnstools-score__checks { list-style: none; margin: 0; padding: 0; display: grid; gap: .4rem; }
    .dnstools-score__checks li {
        display: grid; grid-template-columns: 1.2fr 1fr auto; gap: .75rem;
        padding: .55rem .8rem; border: 1px solid var(--dt-border); border-radius: 10px;
    }
    @media (max-width: 720px) {
        .dnstools-search { grid-template-columns: 1fr; }
        .dnstools-score { flex-direction: column; align-items: flex-start; }
    }
</style>

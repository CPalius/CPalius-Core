{% extends 'admin/layout.html.twig' %}

{% block header %}{{ 'studio.rebuild.header'|trans }}{% endblock %}

{% block javascripts %}
    {{ importmap(['app', 'studio-quick-cache-clear', 'studio-rebuild']) }}
    <script{{ csp_nonce_attr() }} src="{{ cp_asset('studio-rebuild.js') }}" defer></script>
{% endblock %}

{% block body %}
    <div class="space-y-sp-md"
         data-cp-rebuild
         data-cp-rebuild-csrf="{{ csrfToken }}"
         data-i18n-progress="{{ 'studio.rebuild.progress'|trans({percent: '{percent}', done: '{done}', total: '{total}'}) }}"
         data-i18n-done="{{ 'studio.rebuild.done'|trans }}"
         data-i18n-failed="{{ 'studio.rebuild.error'|trans }}"
         data-i18n-request-failed="{{ 'studio.rebuild.request_failed'|trans({error: '{error}'}) }}"
         data-i18n-not-json="{{ 'studio.rebuild.not_json'|trans({status: '{status}'}) }}">

        <p class="text-fs-sm text-neutral-500">{{ 'studio.rebuild.intro'|trans }}</p>

        {% if jobs is empty %}
            <p class="text-fs-sm text-neutral-500">{{ 'studio.rebuild.empty'|trans }}</p>
        {% else %}
            <div class="overflow-hidden rounded-xl border border-neutral-200 bg-white divide-y divide-neutral-100">
                {% for job in jobs %}
                    <div class="flex flex-col gap-3 p-4 sm:flex-row sm:items-center sm:justify-between"
                         data-rebuild-job
                         data-rebuild-id="{{ job.id }}"
                         data-rebuild-url="{{ job.runUrl }}"
                         data-rebuild-total="{{ job.total }}">
                        <div class="min-w-0 flex-1">
                            <h2 class="text-fs-sm font-semibold text-neutral-900">{{ job.name|trans }}</h2>
                            <p class="mt-0.5 text-fs-xs text-neutral-500">{{ job.description|trans }}</p>
                            <div class="mt-3 hidden" data-rebuild-progress-wrap>
                                <div class="h-1.5 overflow-hidden rounded-full bg-neutral-100">
                                    <div class="h-full w-0 rounded-full bg-primary-600 transition-[width] duration-200" data-rebuild-bar></div>
                                </div>
                                <p class="mt-1 text-fs-xs tabular-nums text-neutral-500" data-rebuild-status></p>
                            </div>
                        </div>
                        <button type="button"
                                class="btn btn-sm btn-primary shrink-0 disabled:cursor-wait disabled:opacity-60"
                                data-rebuild-run>
                            {{ 'studio.rebuild.now'|trans }}
                        </button>
                    </div>
                {% endfor %}
            </div>
        {% endif %}
    </div>
{% endblock %}
